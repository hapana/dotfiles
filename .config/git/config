[include]
    path = ./config.user
    # This block should go in ./config.user
    # This isn't committed to keep my email out of git
    # [user]
    #   name="redacted"
    #   email=redacted

[push]
  default=current
